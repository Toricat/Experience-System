import React from 'react';
import LoginForm from '@/app/(auth)/login/form-login';

const LoginPage = () => {
    return (
        <div>
            <h1>Login</h1>
            <LoginForm />
        </div>
    );
};

export default LoginPage;
